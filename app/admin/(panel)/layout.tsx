import type { Metadata } from "next";import { cookies } from "next/headers";import { redirect } from "next/navigation";import AdminSidebar from "@/components/admin/AdminSidebar";import { sessionCookie,verifySession } from "@/lib/admin-auth";
export const metadata:Metadata={title:{default:"Dashboard",template:"%s — Little Ceremony Admin"},robots:{index:false,follow:false}};
export default async function PanelLayout({children}:{children:React.ReactNode}){const session=verifySession((await cookies()).get(sessionCookie)?.value);if(!session)redirect("/admin/login");return <div className="admin-surface"><AdminSidebar name={session.name} role={session.role}/><div className="lg:pl-64 min-h-screen"><div className="p-5 md:p-8 lg:p-10 max-w-[1500px] mx-auto">{children}</div></div></div>}
