import type { Metadata } from "next";import SeoLanding from "@/components/SeoLanding";import { createMetadata } from "@/lib/seo";
export const dynamic="force-dynamic";export const metadata:Metadata=createMetadata({title:"Ubrania świąteczne dla dzieci Małopolska",description:"Eleganckie sukienki, garnitury i ubrania świąteczne dla dzieci. Krótkie serie i szycie na zamówienie.",path:"/ubrania-swiateczne-dla-dzieci"});
export default function Page(){return <SeoLanding config={{title:"Ubrania świąteczne dla dzieci",eyebrow:"Święta i Nowy Rok",intro:"Ciepłe faktury, szlachetne kolory i wygodne fasony do rodzinnych spotkań oraz zimowych sesji zdjęciowych.",local:"Świąteczną stylizację budujemy tak, by jej elementy można było nosić również później — z kamizelką, swetrem lub prostymi dodatkami.",filter:p=>p.occasion.some(x=>x.includes("Święta")||x.includes("Zimowe"))}}/>}
