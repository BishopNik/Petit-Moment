import type { Metadata } from "next";import "./globals.css";import Header from "@/components/Header";import Footer from "@/components/Footer";import MobileActions from "@/components/MobileActions";import AnalyticsTracker from "@/components/AnalyticsTracker";import { siteConfig } from "@/data/siteConfig";import { JsonLd } from "@/components/JsonLd";import { organizationSchema } from "@/lib/schema";
export const metadata:Metadata={metadataBase:new URL(siteConfig.url),title:{default:`${siteConfig.name} — elegancka odzież dziecięca`,template:`%s | ${siteConfig.name}`},description:siteConfig.description,applicationName:siteConfig.name,keywords:["ubrania komunijne Dąbrowa Tarnowska","sukienki komunijne","garnitury dla chłopców Tarnów","szycie dla dzieci Małopolska"],robots:{index:true,follow:true},icons:{icon:"/icon.svg"}};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="pl" data-scroll-behavior="smooth"><body className="mobile-safe"><JsonLd data={organizationSchema}/><AnalyticsTracker/><Header/><main>{children}</main><Footer/><MobileActions/></body></html>}
