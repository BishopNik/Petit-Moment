import type { Metadata } from "next";import Breadcrumbs from "@/components/Breadcrumbs";import BlogCard from "@/components/BlogCard";import { getManagedPosts } from "@/lib/admin-store";import { createMetadata } from "@/lib/seo";
export const dynamic="force-dynamic";export const metadata:Metadata=createMetadata({title:"Poradnik — eleganckie ubrania dla dzieci",description:"Porady o wyborze sukienki komunijnej, garnituru dla chłopca, rozmiaru i przygotowaniu do przymiarki.",path:"/poradnik"});
export default async function Blog(){const posts=await getManagedPosts();return <><header className="soft py-14 md:py-20"><div className="container-site"><Breadcrumbs items={[{label:"Strona główna",href:"/"},{label:"Poradnik"}]}/><p className="eyebrow mt-10">Poradnik Little Ceremony</p><h1 className="display max-w-4xl mt-5">Mniej pośpiechu.<br/><em>Więcej dobrych wyborów.</em></h1><p className="max-w-xl text-[#756b64] leading-relaxed mt-7">O fasonach, rozmiarach, materiałach i tym, jak przygotować dziecko na ważną uroczystość bez niepotrzebnego stresu.</p></div></header><section className="py-16 md:py-24"><div className="container-site grid md:grid-cols-2 lg:grid-cols-3 gap-x-7 gap-y-14">{posts.map(p=><BlogCard key={p.slug} post={p}/>)}</div></section></>}
