import type { Metadata } from "next";import Breadcrumbs from "@/components/Breadcrumbs";import WishlistGrid from "@/components/WishlistGrid";import { getManagedProducts } from "@/lib/admin-store";import { createMetadata } from "@/lib/seo";
export const dynamic="force-dynamic";export const metadata:Metadata=createMetadata({title:"Ulubione produkty",description:"Zapisane sukienki, garnitury i dodatki Little Ceremony Boutique.",path:"/ulubione"});
export default async function Wishlist(){return <><header className="soft py-14 md:py-20"><div className="container-site"><Breadcrumbs items={[{label:"Strona główna",href:"/"},{label:"Ulubione"}]}/><p className="eyebrow mt-10">Twoja lista</p><h1 className="display mt-5">Ulubione<br/><em>stylizacje</em></h1></div></header><section className="py-16 md:py-24"><div className="container-site"><WishlistGrid products={await getManagedProducts()}/></div></section></>}
