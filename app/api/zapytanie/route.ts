import { NextResponse } from "next/server";import { createId,updateStore } from "@/lib/admin-store";import { sendInquiryNotifications } from "@/lib/notifications";
export async function POST(request:Request){const form=await request.formData();const required=["name","phone","email","message","rodo"];if(required.some(key=>!form.get(key)))return NextResponse.json({ok:false,error:"Brak wymaganych pól"},{status:400});const inquiry={id:createId(),createdAt:new Date().toISOString(),name:String(form.get("name")),phone:String(form.get("phone")),email:String(form.get("email")),product:String(form.get("product")||""),size:String(form.get("size")||""),age:String(form.get("age")||""),event:String(form.get("event")||form.get("eventDate")||""),message:String(form.get("message")),formType:String(form.get("formType")||"contact"),status:"Nowe" as const,source:"Website",history:[]};await updateStore(store=>{store.inquiries.unshift(inquiry);store.events.unshift({id:createId(),createdAt:inquiry.createdAt,type:"form_submit",path:"/formularz",label:inquiry.formType,source:"Website",product:inquiry.product||undefined})});await sendInquiryNotifications(inquiry);return NextResponse.json({ok:true,message:"Zgłoszenie zapisane."});}
