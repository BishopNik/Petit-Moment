import { NextResponse } from "next/server";import { authenticate,createSession,sessionCookie } from "@/lib/admin-auth";
export async function POST(request:Request){const {email,password}=await request.json();const user=authenticate(String(email||""),String(password||""));if(!user)return NextResponse.json({ok:false,error:"Nieprawidłowy login lub hasło"},{status:401});const response=NextResponse.json({ok:true,role:user.role});response.cookies.set(sessionCookie,createSession({email:user.email,name:user.name,role:user.role}),{httpOnly:true,sameSite:"strict",secure:process.env.NODE_ENV==="production",maxAge:60*60*12,path:"/"});return response}
