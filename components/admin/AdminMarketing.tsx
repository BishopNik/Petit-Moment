"use client";
import { useState } from "react";import { Plus,Trash,Star } from "@phosphor-icons/react";import type { Promotion,Review } from "@/lib/admin-store";
export default function AdminMarketing({initialReviews,initialPromotions}:{initialReviews:Review[];initialPromotions:Promotion[]}){const [reviews,setReviews]=useState(initialReviews);const [promotions,setPromotions]=useState(initialPromotions);async function addReview(){const item={name:"Nowa klientka",city:"Dąbrowa Tarnowska",text:"Wpisz treść opinii…",rating:5,event:"Komunia",visible:false};const r=await fetch("/api/admin/content",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({collection:"reviews",item})});setReviews([await r.json(),...reviews])}async function addPromotion(){const item={name:"Nowa promocja",description:"Opis promocji",code:"",active:false};const r=await fetch("/api/admin/content",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({collection:"promotions",item})});setPromotions([await r.json(),...promotions])}async function save(collection:"reviews"|"promotions",item:Review|Promotion){const r=await fetch("/api/admin/content",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({collection,item})});const result=await r.json();collection==="reviews"?setReviews(reviews.map(x=>x.id===result.id?result:x)):setPromotions(promotions.map(x=>x.id===result.id?result:x))}async function remove(collection:"reviews"|"promotions",id:string){await fetch(`/api/admin/content?collection=${collection}&id=${id}`,{method:"DELETE"});collection==="reviews"?setReviews(reviews.filter(x=>x.id!==id)):setPromotions(promotions.filter(x=>x.id!==id))}return <div className="grid xl:grid-cols-2 gap-6"><section><div className="flex justify-between items-center"><h2 className="font-semibold">Opinie klientów</h2><button onClick={addReview} className="admin-btn admin-btn-primary"><Plus/> Dodaj</button></div><div className="space-y-3 mt-4">{reviews.map(x=><ReviewRow key={x.id} item={x} save={save} remove={remove}/>)}</div></section><section><div className="flex justify-between items-center"><h2 className="font-semibold">Promocje i kupony</h2><button onClick={addPromotion} className="admin-btn admin-btn-primary"><Plus/> Dodaj</button></div><div className="space-y-3 mt-4">{promotions.map(x=><PromoRow key={x.id} item={x} save={save} remove={remove}/>)}</div></section></div>}
function ReviewRow({item,save,remove}:{item:Review;save:(c:"reviews",x:Review)=>void;remove:(c:"reviews",id:string)=>void}){const [d,setD]=useState(item);return <div className="admin-card p-4 grid gap-3"><div className="grid grid-cols-2 gap-3"><input className="admin-input" value={d.name} onChange={e=>setD({...d,name:e.target.value})}/><input className="admin-input" value={d.city} onChange={e=>setD({...d,city:e.target.value})}/></div><textarea className="admin-input" rows={3} value={d.text} onChange={e=>setD({...d,text:e.target.value})}/><div className="flex justify-between"><label className="text-sm flex items-center gap-2"><input type="checkbox" checked={d.visible} onChange={e=>setD({...d,visible:e.target.checked})}/> Opublikowana</label><div className="flex gap-2"><button onClick={()=>remove("reviews",d.id)} className="admin-btn admin-btn-secondary text-red-600"><Trash/></button><button onClick={()=>save("reviews",d)} className="admin-btn admin-btn-primary">Zapisz</button></div></div></div>}
function PromoRow({item,save,remove}:{item:Promotion;save:(c:"promotions",x:Promotion)=>void;remove:(c:"promotions",id:string)=>void}){const [d,setD]=useState(item);return <div className="admin-card p-4 grid gap-3"><input className="admin-input" value={d.name} onChange={e=>setD({...d,name:e.target.value})}/><textarea className="admin-input" rows={2} value={d.description} onChange={e=>setD({...d,description:e.target.value})}/><input className="admin-input" placeholder="Kod rabatowy" value={d.code} onChange={e=>setD({...d,code:e.target.value})}/><div className="flex justify-between"><label className="text-sm flex gap-2"><input type="checkbox" checked={d.active} onChange={e=>setD({...d,active:e.target.checked})}/> Aktywna</label><div className="flex gap-2"><button onClick={()=>remove("promotions",d.id)} className="admin-btn admin-btn-secondary text-red-600"><Trash/></button><button onClick={()=>save("promotions",d)} className="admin-btn admin-btn-primary">Zapisz</button></div></div></div>}
