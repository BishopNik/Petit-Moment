import Image from "next/image";import type { Product } from "@/data/products";
export default function ProductGallery({product}:{product:Product}){return <div className="grid grid-cols-[1fr_5rem] md:grid-cols-[1fr_7rem] gap-3"><div className="relative aspect-[4/5] bg-[#e9dfd2] overflow-hidden"><Image src={product.image} alt={`${product.name} — widok główny`} fill priority className="object-cover" style={{objectPosition:product.imagePosition||"center"}} sizes="(max-width:1024px) 100vw, 55vw"/></div><div className="flex flex-col gap-3">{["detal materiału","widok sylwetki","inspiracja stylizacji"].map((alt,i)=><div key={alt} className="relative aspect-[4/5] overflow-hidden bg-[#e9dfd2]"><Image src={product.image} alt={`${product.name} — ${alt}`} fill className="object-cover" style={{objectPosition:i===0?"50% 28%":i===1?"50% 65%":"center"}} sizes="120px"/></div>)}</div></div>}
