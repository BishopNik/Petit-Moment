import Image from "next/image";import Link from "next/link";import { ArrowRight } from "@phosphor-icons/react/dist/ssr";import type { BlogPost } from "@/data/blogPosts";
export default function BlogCard({post}:{post:BlogPost}){return <article className="group"><Link href={`/poradnik/${post.slug}`} className="block image-zoom aspect-[5/4] overflow-hidden relative"><Image src={post.image} alt={post.title} fill className="object-cover" sizes="(max-width:768px) 100vw, 33vw"/></Link><div className="pt-5"><p className="text-[10px] uppercase tracking-[.14em] text-[#9a7c58]">{post.category} · {post.readTime}</p><h3 className="serif text-2xl leading-tight mt-2"><Link href={`/poradnik/${post.slug}`}>{post.title}</Link></h3><p className="text-sm text-[#756b64] leading-relaxed mt-3">{post.excerpt}</p><Link href={`/poradnik/${post.slug}`} className="btn-text mt-4">Czytaj <ArrowRight size={14}/></Link></div></article>}
